package es.ulpgc.montesdeoca110.cristina.sprint;

import java.util.Objects;

public class ContadorViewModel {

    public String data = "0";
    public int numData = 0;

   /* @Override
    public boolean equals(Object obj) {
        if (this == obj) return true;
        if (obj == null || getClass() != obj.getClass()) return false;
        ContadorViewModel viewModel = (ContadorViewModel) obj;
        return progressBarVisibility == viewModel.progressBarVisibility &&
                Objects.equals(helloMessage, viewModel.contadorMessage);
    }*/
}
